[build]
  command = "npm run build"
  publish = "dist"  # Note: Vite uses 'dist', not 'build'

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[build.environment]
  NODE_VERSION = "18"